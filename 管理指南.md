# SWPE 授權名單管理指南

## 前置條件

每次使用 SWPE.exe 前，**必須先開啟 Fiddler** 並確認 Auto-Responder 規則已啟用。

---

## 日常管理指令

所有指令在 `c:\Users\User\Desktop\swpe\auth_repo` 目錄下執行。

### 📋 列出所有用戶

```powershell
python manage.py list
```

### ➕ 新增用戶

```powershell
python manage.py add 名稱 類型 用戶ID 到期日(YYYY-MM-DD)
```

**範例：**
```powershell
python manage.py add 小明 New::137 123456 2027-06-30
python manage.py add 永久帳號 New::136 999001 2099-12-31
```

**類型說明：**
| 類型 | 說明 |
|------|------|
| `New::136` | 類型 136 |
| `New::137` | 類型 137（最常見） |
| `New::138` | 類型 138 |

### ➖ 移除用戶

```powershell
python manage.py remove 名稱
```

**範例：**
```powershell
python manage.py remove 猛將
```

### 🔄 產生加密檔案（僅本地）

```powershell
python manage.py generate
```

### 🚀 產生 + 推送到 GitHub（完整更新）

```powershell
python manage.py push
```

> ⚠️ 每次修改用戶後，**一定要執行 push** 才會生效！

---

## 完整操作流程範例

### 新增一個用戶

```powershell
cd c:\Users\User\Desktop\swpe\auth_repo
python manage.py add 新玩家 New::137 555555 2028-12-31
python manage.py push
```

### 移除一個用戶

```powershell
cd c:\Users\User\Desktop\swpe\auth_repo
python manage.py remove 猛將
python manage.py push
```

### 批量修改

直接編輯 `users.json`，修改後執行：

```powershell
cd c:\Users\User\Desktop\swpe\auth_repo
python manage.py push
```

---

## users.json 格式說明

```json
{
    "name": "用戶名稱",
    "type": "New::137",
    "id": 123456,
    "expire": "2030-12-31"
}
```

| 欄位 | 說明 | 範例 |
|------|------|------|
| `name` | 用戶名稱 | `"小明"` |
| `type` | 授權類型 | `"New::137"` |
| `id` | 用戶 ID（數字） | `123456` |
| `expire` | 到期日 (YYYY-MM-DD) | `"2030-12-31"` |

> 永久授權請填 `"2099-12-31"`

---

## 使用 SWPE.exe 的步驟

1. 開啟 **Fiddler**（確認 Auto-Responder 規則已啟用）
2. 啟動 **SWPE.exe**
3. 使用完畢後可以關閉 Fiddler

---

## GitHub Repo

- 位置：https://github.com/pinxunchen/swpe-auth
- 加密檔案路徑：`Reliable/Reversible`
- 用戶清單：`users.json`
